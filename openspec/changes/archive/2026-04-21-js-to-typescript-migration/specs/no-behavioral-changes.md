_This is a pure refactor (JS -> TS conversion). No new or modified capabilities are introduced. The proposal explicitly lists zero capability changes._

_The only invariant is that the public API surface and runtime behavior remain identical after migration. This is verified by the existing test suite and successful build._
